<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>DevOps Glossary</title>

  <!-- Bootstrap CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>

<body class='p-5'>
  <div class='my-5'>
    <h2 class="mt-2">SIT722 DevOps Glossary</h2>
    <p class='fs-5 fw-normal fst-italic'>A collection of essential DevOps terms and their definitions to help you
      navigate the
      world of DevOps.</p>
  </div>

  <div class="table-responsive">
    <table class="table table-bordered align-middle">
      <thead class="table-secondary">
        <tr>
          <th>ID</th>
          <th>Term</th>
          <th>Definition</th>
          <th>Reference</th>
        </tr>
      </thead>
      <tbody>
        <% glossary.forEach((item, index)=> { %>
          <tr>
            <td>
              <%= index + 1 %>
            </td>
            <td nowrap>
              <%= item.term %>
            </td>
            <td>
              <%= item.definition %>
            </td>
            <td class='mw-25'>
              <% item.references.forEach(url=> { %>
                <div class="my-2">
                  <%= url %>
                </div>
                <% }); %>
            </td>
          </tr>
          <% }); %>
      </tbody>
    </table>
  </div>

  <!-- Bootstrap JS (Optional, for certain features like dropdowns) -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>